###############################################################################
# AIOP — 中央スタックの環境変数 (LiteLLM が参照)
#
#   このファイルは雛形です。`make bootstrap` (= scripts/gen-env.sh) を実行すると
#   シークレットが自動生成された .env が作られます。手動で作る場合は
#   `cp .env.example .env` して PROVIDER キー等を埋めてください。
#
#   重要: .env は機密です。git にコミットしないこと (.gitignore 済み)。
###############################################################################

# =============================================================================
# 1. LLM プロバイダ (LiteLLM の背後)
# =============================================================================
# Anthropic Claude — https://console.anthropic.com/ で取得
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Vertex AI (Gemini埋め込み) — APIキーではなく ADC を使用
#   ホストで: gcloud auth application-default login
#   ~/.config/gcloud がコンテナにマウントされ ADC で認証される (静的キー不要)
VERTEX_PROJECT=your-gcp-project-id
VERTEX_LOCATION=us-central1

# ローカル Ollama。Docker Desktop(mac/win)では host.docker.internal が host を指す。
# Linux では LiteLLM サービスに extra_hosts:host-gateway を付与済み。
# Ollama をコンテナ化したい場合は別途 compose に追加し、ここを http://ollama:11434 に。
OLLAMA_API_BASE=http://host.docker.internal:11434

# =============================================================================
# 2. LiteLLM ゲートウェイ (統制レイヤー)
# =============================================================================
# 管理マスターキー。これで /key, /team, /budget 等の管理APIと UI にアクセス。 (sk- で始める)
LITELLM_MASTER_KEY=sk-CHANGE-ME-master-key
# DB に保存するプロバイダキーを暗号化する鍵
LITELLM_SALT_KEY=CHANGE-ME-litellm-salt
# 管理 UI ( http://localhost:4000/ui ) のログイン
LITELLM_UI_USERNAME=admin
LITELLM_UI_PASSWORD=CHANGE-ME-litellm-ui-pass
# LiteLLM 用 Postgres のパスワード
LITELLM_DB_PASSWORD=CHANGE-ME-litellm-db-pass
# ホスト公開ポート
LITELLM_PORT=4000
# LiteLLM を公開するホストのインタフェース (セキュリティ)。
#   既定 172.17.0.1 = docker bridge gateway。ネイティブ Linux docker では
#   Dify コンテナ内の host.docker.internal がこの IP を指すため、ここにバインドすると
#   全 Dify から到達でき、かつ LAN には露出しない (172.17.0.0/16 はホスト内部網)。
#   ※ docker0 のサブネットを変更している場合はそのゲートウェイIPに合わせる
#     (確認: `docker network inspect bridge -f '{{(index .IPAM.Config 0).Gateway}}'`)。
#   ※ Docker Desktop / WSL2 backend を使う場合は 127.0.0.1 でも到達する。
LITELLM_HOST=172.17.0.1

# =============================================================================
# 3. Gateway (front-nginx + Keycloak + oauth2-proxy) — EntraID 認証 / 公開範囲制御
#    ※ compose.gateway.yaml / gateway/ が参照。使わない場合は無視してよい。
# =============================================================================
# 公開ドメイン。サブドメインをこの下に生やす:
#   auth.<GATEWAY_DOMAIN> = Keycloak / <team>.<GATEWAY_DOMAIN> = 各 Dify インスタンス
GATEWAY_DOMAIN=example.com
# Keycloak realm 名
KEYCLOAK_REALM=aiop
# 認可グループの接頭辞。gateway-add.sh (作成側) と gateway-grant.sh (割当側) が
# 同じ値を見る必要があるため .env に一元化する (グループ名 = /<接頭辞>-<team>)。
#   ※ 生成済みの gateway/oauth2-proxies.gateway.yaml には作成時の値が焼き込まれる。
#     後から変更する場合は既存チームの --allowed-group も合わせて直すこと。
KEYCLOAK_GROUP_PREFIX=aiop
# Keycloak 管理者 (初回ブートストラップ)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=CHANGE-ME-kc-admin
# Keycloak 用 Postgres パスワード
KEYCLOAK_DB_PASSWORD=CHANGE-ME-kc-db
# oauth2-proxy の cookie 署名鍵 (全チーム共通)。鍵長は 16/24/32 byte 必須。
#   base64(32)=44文字は oauth2-proxy に弾かれるため 32文字hex を使う。
#   生成例: openssl rand -hex 16
OAUTH2_PROXY_COOKIE_SECRET=CHANGE-ME-cookie-secret
# --- 動作モード (既定は従来どおり: EntraID SSO + front-nginx で TLS 終端) ---
#   GATEWAY_AUTH  sso  = oauth2-proxy + Keycloak で認証/認可 (本番)
#                 none = 認証なしで Dify へ素通し。Keycloak/oauth2-proxy を起動しない。
#                        リバースプロキシ (サブドメイン → Dify ポート) だけを使う構成。
#                        ⚠ 到達できる人は全員 Dify を開ける。閉じた網か、前段で認証する場合のみ。
GATEWAY_AUTH=sso
#   GATEWAY_TLS   terminate = この front-nginx が 443 で TLS 終端する (証明書が必要)
#                 none      = TLS は前段 (別の nginx / ALB / Cloudflare 等) で終端済み。
#                             front-nginx は 80 で平文待ち受け、443 は公開しない。
#                             X-Forwarded-Proto/Port は前段の値をそのまま尊重する。
#   ※ 現状 GATEWAY_TLS=none は GATEWAY_AUTH=none (素通し) との組み合わせのみ対応。
GATEWAY_TLS=terminate
# GATEWAY_TRUSTED_PROXY_IP: front-nginx (GATEWAY_TLS=terminate) の手前に、TLSを終端せず
#   TCP/SNI のまま転送するだけの別プロキシ (社内の既存リバースプロキシ等) がいる場合に設定する。
#   何も無ければ空のままでよい (既定・最も一般的な構成)。
#
#   このような前段プロキシは通常「新しい別のTCP接続」を front-nginx へ張るため、実接続元IPが
#   前段プロキシ自身のIPに置き換わる (全ユーザーが同一IPに見え、Keycloak監査ログ等が使えなくなる)。
#   ここに前段プロキシのIP (カンマ区切りで複数可、CIDR可: 例 10.33.37.5,10.33.38.0/24) を
#   設定すると、front-nginx が PROXY protocol を受け付け、実接続元IPを復元する。
#   ※ 前段プロキシ側でも PROXY protocol の送出を有効にする必要がある (両側同時に切り替えること。
#     片側だけ有効化すると通信できなくなる)。
#   ※ GATEWAY_TLS=terminate のときのみ意味を持つ (GATEWAY_TLS=none は別の仕組みで前段の値を
#     引き継ぐため対象外)。
#   ※ 443 (TLS) の listen にのみ適用される。80 (http→https リダイレクト等) は対象外
#     (SNI での振り分けは TLS ハンドシェイクが前提のため、前段プロキシ側でも通常 443 だけが
#     この方式になる)。前段プロキシが 80 にも PROXY protocol を付けて転送する構成なら、
#     gateway/nginx/templates 等の `listen 80` 側にも同様の対応が別途必要になるので相談すること。
#   ※ IPv4 のみ対応 (IPv6 の前段プロキシは非対応)。
GATEWAY_TRUSTED_PROXY_IP=
# --- EntraID (Azure AD) OIDC ブローカー ---
#   Azure ポータル > アプリの登録 で取得。リダイレクト URI は初期化時に案内される:
#     https://auth.<GATEWAY_DOMAIN>/realms/<KEYCLOAK_REALM>/broker/entraid/endpoint
ENTRA_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ENTRA_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ENTRA_CLIENT_SECRET=CHANGE-ME-entra-client-secret
